---
import CommonContent from "./CommonContent.astro";
import ContentCard from "./ContentCard.astro";

const { entry, ...extraProps } = Astro.props;
const slug = `/musings/${entry.data.slug}`;
---

<ContentCard slug={slug} {...extraProps} extraStyle="font-serif">
  <CommonContent
    entry={entry}
    toolbarText={entry.data.category}
    toolbarStyle="text-gray-500 text-sm"
    titleStyle="-mb-1 pb-1 font-medium not-italic text-gray-800 decoration-wavy group-hover:decoration-category-musings-mark"
    subTitleStyle="text-gray-500 not-italic"
    tagText="musings"
    tagColor="bg-category-musings-tint text-category-musings-text border-category-musings-border"
  >
    <div class="line-clamp-2">{entry.data.excerpt}</div>
  </CommonContent>
</ContentCard>
