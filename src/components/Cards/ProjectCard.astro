---
import { Image } from "astro:assets";
import { joinTags } from "../../utils/cardUtils";
import CommonContent from "./CommonContent.astro";
import ContentCard from "./ContentCard.astro";

const { entry } = Astro.props;
const slug = `/projects/${entry.data.slug}`;
---

<ContentCard slug={slug}>
  <div class="flex flex-col h-full">
    <CommonContent
      entry={entry}
      toolbarText={joinTags(entry)}
      toolbarStyle="text-xs uppercase tracking-wide text-stone-600"
      titleStyle="text-gray-900 font-semibold group-hover:decoration-category-project-mark"
      subTitleStyle="hidden"
      tagText="project"
      tagColor="bg-category-project-tint text-category-project-text border-category-project-border"
    />
    <div class="overflow-clip rounded-cards">
      <Image
        src={entry.data.cover}
        widths={[240, 540, 720]}
        alt=""
        class="max-h-20 object-cover object-center h-full w-full"
      />
    </div>
  </div>
</ContentCard>
