---
const { slug, extraStyle = "" } = Astro.props;
---

<div
  class={`overflow-clip bg-darken group border border-edge hover:border-extralight rounded-cards transition duration-300 shadow-md hover:shadow-xl h-full ${extraStyle}`}
>
  <a href={slug}>
    <slot />
  </a>
</div>
